import styled from "styled-components";

export const ProductsSkeletonContainer = styled.div`
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 22px;
    align-items: center;
    max-width: 1000px;
    margin: 0 auto;
`
